name: Lint

on: [push]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
      - name: Install the project
        run: uv sync
      - name: Pylint and Pycodestyle
        run: |
          uv run pylint pytamaro
          uv run pycodestyle pytamaro tests
